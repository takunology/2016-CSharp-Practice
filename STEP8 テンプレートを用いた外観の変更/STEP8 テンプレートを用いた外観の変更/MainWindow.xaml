<Window x:Class="STEP8_テンプレートを用いた外観の変更.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:STEP8_テンプレートを用いた外観の変更"
        mc:Ignorable="d"
        Title="MainWindow" Height="100" Width="250">
    <Window.Resources>
        <Style x:Key="EllipseButton" TargetType="Button">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid>
                            <Ellipse Fill="{TemplateBinding Background}" />
                            <ContentPresenter Margin="{TemplateBinding Padding}" RecognizesAccessKey="True" HorizontalAlignment="Center"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <StackPanel>
        <Button Style="{StaticResource EllipseButton}" Content="ボタン1" Background="Gray" Click="Button_Click_1"/>
        <Button Style="{StaticResource EllipseButton}" Padding="2" Content="ボタン2" Background="LightGray" Click="Button_Click"/>
    </StackPanel>
</Window>
